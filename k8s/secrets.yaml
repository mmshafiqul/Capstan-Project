apiVersion: v1
kind: Secret
metadata:
  name: go-service-secrets
  namespace: urlshortner
type: Opaque
stringData:
  # Stored for assignment/demo purposes. NOTE: the current app does NOT use Redis AUTH.
  # If you enable Redis AUTH (requirepass) you must also update the services to use it.
  REDIS_PASSWORD: "Admin123!"
---
apiVersion: v1
kind: Secret
metadata:
  name: python-service-secrets
  namespace: urlshortner
type: Opaque
stringData:
  REDIS_PASSWORD: "Admin123!"
---
apiVersion: v1
kind: Secret
metadata:
  name: node-service-secrets
  namespace: urlshortner
type: Opaque
stringData:
  # Node service does not require secrets for this project; keep a placeholder to satisfy the assignment.
  DUMMY_SECRET: "changeme"
